[package]
name = "app-shell"
version = "0.1.0"
authors = ["hackartists <hackartists@gmail.com>"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
dioxus = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
serde_repr = "0.1.20"
serde_with = { workspace = true }
tower-sessions = { version = "0.15.0", optional = true }
icons = { workspace = true }
percent-encoding = { workspace = true }
aws-config = { version = "1", optional = true }
aws-sdk-s3 = { version = "1", optional = true }
aws-sdk-bedrockruntime = { workspace = true, optional = true }
aws-sdk-bedrockagentruntime = { version = "1", optional = true }
aws-smithy-types = { version = "1", optional = true }
aws-sdk-dynamodb = { workspace = true, optional = true }
aws-sdk-dynamodbstreams = { workspace = true, optional = true }
rdkafka = { version = "0.38", features = ["cmake-build", "tokio"], optional = true }
qdrant-client = { workspace = true, optional = true }
tracing = { workspace = true }
gloo-timers = { version = "0.3", features = ["futures"], optional = true }
serde_dynamo = { workspace = true, optional = true }
tauri-common = {workspace = true, optional = true, features = ["web"] }
base64 = { workspace = true }
# `js` feature: source of randomness on `wasm32-unknown-unknown` for `Uuid::now_v7()`.
uuid = { workspace = true }
chrono = { workspace = true }
regex = { version = "1" }
ethers = { version = "=2.0.14", optional = true }
names = { version = "0.14.0", optional = true }
rand = { version = "0.10" }
rand_chacha = { version = "0.9", optional = true }
once_cell = { version = "1" }
lindera = { version = "3", default-features = false, features = ["embed-ko-dic"], optional = true }
futures = { version = "0.3", optional = true }
urlencoding = { version = "2" }
tokio = { workspace = true, optional = true }
tower = { version = "0.5", features = ["util"], optional = true }
tower-http = { version = "0.6", features = ["catch-panic", "cors"], optional = true }
pulldown-cmark = { version = "0.12", default-features = false, features = ["html"], optional = true }

web-sys = { workspace = true, features = [
  "Blob",
  "Clipboard",
  "Document",
  "Element",
  "File",
  "Headers",
  "IntersectionObserver",
  "IntersectionObserverEntry",
  "IntersectionObserverInit",
  "Navigator",
  "Request",
  "RequestCredentials",
  "RequestInit",
  "Response",
  "Window",
  "console",
] }
wasm-bindgen = { workspace = true }
wasm-bindgen-futures = { workspace = true }
serde-wasm-bindgen = { workspace = true }
random-string = { version = "1.1.0", optional = true }
reqwest = { workspace = true, optional = true }
jsonwebtoken = { workspace = true, optional = true }
async-trait = { workspace = true }
sha3 = { version = "0.10.8", optional = true }
sha2 = { version = "0.10", optional = true }
aes-gcm = { version = "0.10", optional = true }
hex = { version = "0.4.3", optional = true }
k256 = { version = "0.13", features = ["ecdsa"], optional = true }
hmac = { version = "0.12.1", optional = true }
url = { version = "2", optional = true }
validator = { version = "0.20.0", features = ["derive"], optional = true }
aws-sdk-sns = { version = "1.83.0", optional = true }
aws-sdk-sesv2 = { version = "1.83.0", optional = true }
thiserror = { workspace = true }

# Dependencies from common
by-macros = { workspace = true }
dioxus-translate = { workspace = true }
lucide-dioxus = { workspace = true }
ssi = { version = "0.12.0", features = ["bbs"], optional = true }
aws-credential-types = { version = "1.2.6", optional = true }
strum = { version = "0.27.2", features = ["derive"] }
js-sys = "0.3"
# Installed in tauri-web main.rs so release-wasm panics print location + message
# to console instead of bubbling up as opaque "RuntimeError: unreachable".
console_error_panic_hook = { version = "0.1", optional = true }
tracing-wasm = "0.2.1"
dioxus-cli-config = "0.7.3"
tracing-subscriber = { workspace = true }
getrandom = { version = "0.4", features = ["wasm_js"], optional = true }
lambda_http = { version = "1.0.2", optional = true }
dioxus-primitives = { git = "https://github.com/DioxusLabs/components", version = "0.0.1", default-features = false, features = [
  "router",
] }
manganis = { workspace = true }
time = { version = "0.3.47", features = ["formatting"] }
ic-agent = { version = "0.46", optional = true }
candid = { version = "0.10", optional = true }
ratel-canister = { path = "../../packages/ratel-canister", default-features = false, features = [
  "dto",
], optional = true }
attr-voting = { path = "../../packages/attr-voting", optional = true }
mimetype-detector = "0.3.6"
lambda_runtime = { version = "1.1.1", optional = true }
rmcp = { version = "0.7.0", git = "https://github.com/modelcontextprotocol/rust-sdk", features = [
    "server",
    "transport-streamable-http-server",
    "transport-worker",
    "macros",
], optional = true }
strip-tags = "0.1.0"
generational-box = "0.7.9"

[features]
default = ["web","server"]
bypass = []
# Note: `fullstack` is NOT included here so `--platform web` builds for the
# Tauri Android shell (which auto-adds `web` and supplies its own server-fn
# shims) don't transitively enable `dioxus-web/hydrate`. That hydration code
# tries to consume an SSR-serialized initial-state buffer that the Tauri
# WebView never produces, leaving the WebsysDom in a state where the wasm
# emits DOM ops referencing un-mounted nodes and JS throws
# `Cannot read properties of undefined (reading 'listening')`. The regular
# web build adds `fullstack` explicitly via `dx build ... --features web,fullstack`.
web = ["dioxus/web", "getrandom", "gloo-timers", "time/wasm-bindgen", "attr-voting", "hex", "sha2", "uuid/js", "dep:console_error_panic_hook"]
fullstack = ["dioxus/fullstack"]
desktop = ["dioxus/desktop"]
mobile = ["dioxus/mobile", "tokio", "dep:console_error_panic_hook", "dep:reqwest", "dioxus/web", "getrandom", "gloo-timers", "time/wasm-bindgen", "attr-voting", "hex", "sha2", "uuid/js"]
# Type-only feature: compiles app/ratel/src/tauri/types/ and nothing else.
# Pulled by app/ratel-tauri so the native shell shares DTO definitions with the
# web bundle. MUST NOT bring in dioxus, reqwest, or any web/server-only deps.
# Web build for the Tauri Android shell. Additive on `web` so the dioxus-web
# code path is identical; only adds compilation of app/ratel/src/tauri/interop/
# (the JS-eval drivers that call window.__TAURI__.invoke).
tauri-web = ["tauri-common", "dep:console_error_panic_hook", "dep:reqwest", "dioxus/web", "getrandom", "gloo-timers", "time/wasm-bindgen", "attr-voting", "hex", "sha2", "uuid/js"]
server = [
  "fullstack",
  "aws-config",
  "aws-credential-types",
  "aws-sdk-bedrockagentruntime",
  "aws-sdk-bedrockruntime",
  "aws-sdk-dynamodb",
  "aws-sdk-dynamodbstreams",
  "aws-sdk-s3",
  "aws-sdk-sesv2",
  "aws-sdk-sns",
  "aws-smithy-types",
  "candid",
  "dioxus/server",
  "dioxus/fullstack",
  "ethers",
  "futures",
  "hex",
  "lindera",
  "rand_chacha",
  "hmac",
  "ic-agent",
  "k256",
  "names",
  "qdrant-client",
  "random-string",
  "dep:reqwest",
  "dep:jsonwebtoken",
  "ratel-canister",
  "serde_dynamo",
  "sha2",
  "aes-gcm",
  "attr-voting",
  "sha3",
  "ssi",
  "tokio",
  "tower",
  "tower-http",
  "tower-sessions",
  "url",
  "validator",
  "rmcp",
]

lambda = ["lambda_http", "lambda_runtime", "server"]
# Redpanda event pipeline (k3s migration): CDC producer + worker binaries.
# Never enabled by web/mobile/lambda/default server builds.
redpanda = ["server", "dep:rdkafka"]
layout_test = ["web"]
local-dev = ["beta","dep:pulldown-cmark"]
beta = []
futures = ["dep:futures"]
lindera = ["dep:lindera"]
rand_chacha = ["dep:rand_chacha"]

# Android-only: route tracing events into logcat so they show up in `adb logcat`.
# Without this, Rust `tracing::info!` / `error!` calls silently vanish on device
# because the default fmt subscriber writes to stdout, which is not captured by
# logcat on Android.
[target.'cfg(target_os = "android")'.dependencies]
tracing-android = "0.2"

[dev-dependencies]
tower = { version = "0.5" }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
